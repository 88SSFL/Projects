def consistence(k):
    a=list()
    b=list(k)
    c=['(','[','{',')',']','}']
    d=0
    e='True'
    while True:
        if d==len(b):
            break
        f=b[d]
        if f in c[:3]:
            a.append(b[d])
        elif f in c[3:] and (len(a)==0 or a[-1]!=c[c.index(f)-3]):
            e='False'
            break
        elif f in c[3:] and a[-1]==c[c.index(f)-3]:
            a=a[:-1]
        d+=1
    if len(a)!=0:
        e='False'
    return print(e)
